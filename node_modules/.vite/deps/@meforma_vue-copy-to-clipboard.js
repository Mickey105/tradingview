import "./chunk-THBTSZW5.js";

// node_modules/@meforma/vue-copy-to-clipboard/index.js
var vue_copy_to_clipboard_default = {
  install: (app) => {
    app.config.globalProperties.$copyText = (text) => {
      if (!navigator.clipboard) {
        const textArea = document.createElement("textarea");
        textArea.value = text;
        textArea.style.top = "0";
        textArea.style.left = "0";
        textArea.style.position = "fixed";
        document.body.appendChild(textArea);
        textArea.focus();
        textArea.select();
        try {
          document.execCommand("copy");
          document.body.removeChild(textArea);
          return Promise.resolve();
        } catch (err) {
          document.body.removeChild(textArea);
          return Promise.reject(err);
        }
      }
      return navigator.clipboard.writeText(text);
    };
  }
};

// dep:@meforma_vue-copy-to-clipboard
var meforma_vue_copy_to_clipboard_default = vue_copy_to_clipboard_default;
export {
  meforma_vue_copy_to_clipboard_default as default
};
//# sourceMappingURL=@meforma_vue-copy-to-clipboard.js.map
